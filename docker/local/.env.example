# PrinterHub — local Docker environment
# Copy this file to .env and adjust values if needed.
# Defaults in docker-compose.yml will be used if these are not set.

DB_USER=printerhub
DB_PASSWORD=printerhub_secret
